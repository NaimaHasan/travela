import 'package:flutter/material.dart';

class DestinationListScreenDesktop extends StatelessWidget {
  const DestinationListScreenDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
